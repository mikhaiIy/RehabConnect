@model RehabConnect.Models.ViewModel.HomeVM

@{
    ViewData["Title"] = "Customer Support";
}

@section VendorStyles {
    <link rel="stylesheet" href="~/vendor/libs/apex-charts/apex-charts.css" />
    <link rel="stylesheet" href="~/vendor/libs/swiper/swiper.css" />
}

@section VendorScripts {
    <script src="~/vendor/libs/apex-charts/apexcharts.js"></script>
    <script src="~/vendor/libs/swiper/swiper.js"></script>
}

@section PageStyles {
    <link rel="stylesheet" href="~/vendor/css/pages/cards-advance.css" />
}

@section PageScripts {
    <script src="~/js/dashboards-analytics.js"></script>
    <script src="~/js/ui-carousel.js"></script>
    <script src="~/js/dashboards-crm.js"></script>
}

<div class="container-fluid">

    <!-- Dashboard Cards Section -->
    <div class="row">
        <!-- Parents Card -->
        <div class="col-xl-2 col-md-4 col-6 mb-4">
            <div class="card">
                <div class="card-header pb-0">
                    <h5 class="card-title mb-0">Parents</h5>
                    <small class="text-muted">Total Count</small>
                </div>
                <div class="card-body pt-0">
                    <div class="d-flex justify-content-between align-items-center mt-3 gap-3">
                        <h5 class="mb-0">@Model.ParentDetails.Count()</h5>
                    </div>
                </div>
            </div>
        </div>

        <!-- Total Count Student -->
        <div class="col-xl-2 col-md-4 col-6 mb-4">
            <div class="card">
                <div class="card-header pb-0">
                    <h5 class="card-title mb-0">Students</h5>
                    <small class="text-muted">Total Count</small>
                </div>
                <div class="card-body pt-0">
                    <div class="d-flex justify-content-between align-items-center mt-3 gap-3">
                        <h5 class="mb-0">@Model.Students.Count()</h5>
                    </div>
                </div>
            </div>
        </div>

        <!-- Total Count Therapist -->
        <div class="col-xl-2 col-md-4 col-6 mb-4">
            <div class="card">
                <div class="card-header pb-0">
                    <h5 class="card-title mb-0">Therapists</h5>
                    <small class="text-muted">Total Count</small>
                </div>
                <div class="card-body pt-0">
                    <div class="d-flex justify-content-between align-items-center mt-3 gap-3">
                        <h5 class="mb-0">@Model.Therapists.Count()</h5>
                    </div>
                </div>
            </div>
        </div>

        <!-- Total Count Customer Support -->
        <div class="col-xl-2 col-md-4 col-6 mb-4">
            <div class="card">
                <div class="card-header pb-0">
                    <h5 class="card-title mb-0">C.Support</h5>
                    <small class="text-muted">Total Count</small>
                </div>
                <div class="card-body pt-0">
                    <div class="d-flex justify-content-between align-items-center mt-3 gap-3">
                        <h5 class="mb-0">@Model.CustomerSupports.Count()</h5>
                    </div>
                </div>
            </div>
        </div>

    </div>

    <!-- Announcements Section -->
    <div class="row">
        <div class="col-10 col-xl-8 mb-4">
            <div class="card">
                <div class="card-body">
                    <div id="carouselExampleDark" class="carousel carousel-dark slide carousel-fade" data-bs-ride="carousel">
                        <div class="carousel-indicators">
                            @for (int i = 0; i < Model.Announcements.Count(); i++)
                            {
                                <button type="button" data-bs-target="#carouselExampleDark" data-bs-slide-to="@i" class="@((i == 0) ? "active" : "")" aria-current="true" aria-label="Slide @i"></button>
                            }
                        </div>
                        <div class="carousel-inner">
                            @foreach (var announcement in Model.Announcements)
                            {
                                <div class="carousel-item @(announcement == Model.Announcements.First() ? "active" : "")">
                                    <img class="d-block w-100" src="@announcement.ImageUrl" alt="@announcement.Title" />
                                </div>
                            }
                        </div>
                        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleDark" data-bs-slide="prev">
                            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                            <span class="visually-hidden">Previous</span>
                        </button>
                        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleDark" data-bs-slide="next">
                            <span class="carousel-control-next-icon" aria-hidden="true"></span>
                            <span class="visually-hidden">Next</span>
                        </button>
                    </div>
                </div>
            </div>
        </div>

    </div>

</div>
